#include "./qalgo_utility.h"
#include <assert.h>
#include "../include/qalgorithms_qpeaks.h"

int main(int argc, char const *argv[])
{
    /* code */
    assert(argc == 2);

    char coeffs[64]; // all coeffs with 15 digits
    printf();
    return 0;
}
